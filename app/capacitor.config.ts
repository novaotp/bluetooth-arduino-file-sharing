import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bafs.app',
  appName: 'Bluetooth Arduino File Sharing',
  webDir: 'build'
};

export default config;
